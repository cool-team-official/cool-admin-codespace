FROM mcr.microsoft.com/devcontainers/base:0.203.1-bullseye
RUN apt update && apt install -y \
    python3 \
    sqlite3 \
    python3-pip 
    # iputils-ping \
    # traceroute \
    # telnet 
# COPY daemon.json /etc/docker/daemon.json
